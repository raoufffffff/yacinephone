import data from '../config.json'
const { email, facebook, instgarm, logo, main_color, phone, store_name, tiktok, id } = data
export default { email, facebook, instgarm, logo, main_color, phone, store_name, tiktok, id } 